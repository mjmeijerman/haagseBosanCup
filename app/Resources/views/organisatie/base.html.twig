<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Strict//EN" "http://www.w3.org/TR/xhtml1/DTD/xhtml1-strict.dtd">
<html xmlns="http://www.w3.org/1999/xhtml">
<head>
    <title>Haagse Bosan Cup</title>
    <meta charset="UTF-8"/>
    {% block stylesheets %}
        <link href="{{ asset('css/organisatie.css') }}" rel="stylesheet"/>
    {% endblock %}
    <link rel="shortcut icon" type="image/png" href="{{ asset('images/favicon.png') }}"/>
    <script src="{{ asset('js/jquery.js') }}"></script>
    <script src="{{ asset('bundles/fosjsrouting/js/router.js') }}"></script>
    <script src="{{ path('fos_js_routing_js', {'callback': 'fos.Router.setData'}) }}"></script>
</head>
<body>
<center>
    <div id="container">
        <header>
            <a href="{{ path('organisatieGetContent', { 'page': 'Home' }) }}) }}">
                <img src="{{ asset('images/header_organisatie.png') }}" alt=""/>
            </a>
        </header>

        <nav id="menu">
            {% for items in menuItems.hoofdmenuItems %}
                <a href="{{ path('organisatieGetContent', { 'page': items.naam }) }}">{{ items.naam }}</a>
            {% endfor %}
            <a href="{{ path('logout') }}">Uitloggen</a>
        </nav>

        <div id="statistiek">
            0 VERENIGINGEN | 6 TURNSTERS | 4 WACHTLIJST | 1 JURYLID
        </div>

        <article class="index">
            {% block body %}{% endblock %}
        </article>

        <aside>
            <h1>To do</h1>
            <ul>
                <li><s>Ding 1</s>
                    <ul>
                        <li>Subding 1</li>
                        <li>Subding 2</li>
                    </ul>
                </li>
                <li>Ding 3</li>
                <li>Ding 4</li>
            </ul>
        </aside>

    </div>
</center>
{% block javascripts %}

{% endblock %}
</body>
</html>